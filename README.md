# C-like-language-compiler-in-Python

📖 Описание
Данный проект представляет собой учебный компилятор, преобразующий код на C-подобном языке в эквивалентный Python код. Компилятор реализован с использованием Flex (лексический анализатор) и Bison (синтаксический анализатор).

🎯 Основные возможности
Поддержка базовых типов данных: int, double
Арифметические операции: +, -, *, /, %, //
Битовые операции: &, |, ^, <<, >>
Математические функции: sin, cos, tan, pow, sqrt, log, exp и другие
Функции: объявление и вызов функций с параметрами
Преобразование типов: явное приведение (int) и (double)
Вывод данных: преобразование printf в Python print

🛠 Технологии
Flex - лексический анализатор
Bison - синтаксический анализатор
C - язык реализации компилятора
Python - целевой язык компиляции

🚀 Установка и запуск
Требования
Flex
Bison
GCC
Python 3.x

Компиляция компилятора
# Генерация лексера и парсера
flex sin.l
bison -dy -v sint.y
# Компиляция компилятора
gcc lex.yy.c y.tab.c -o result.exe
./result

📝 Пример использования
Входной код (C-подобный)
double complexMath(double x, double y) {
    return sin(x) * cos(y) + pow(x, y) - sqrt(fabs(x - y));
}

int main() {
    double result = complexMath(2.0, 3.0);
    printf("Результат: %.3f\n", result);
    return 0;
}

Выходной код (Python)
import math

def complexMath(x, y):
    return (math.sin(x) * math.cos(y) + math.pow(x, y) - math.sqrt(math.fabs(x - y)))

def main():
    result = complexMath(2.0, 3.0)
    print("Результат: %.3f" % (result))
    return 0

# Вызов main функции
if __name__ == "__main__":
    main()

⚠️ Ограничения
Компилятор поддерживает ограниченное подмножество C, включая:

✅ Поддерживается:
Базовые типы данных (int, double)
Арифметические и битовые операции
Математические функции
Функции с параметрами
Присваивания и выражения

❌ Не поддерживается:
Указатели и массивы
Структуры и объединения
Циклы (for, while)
Условные операторы (if-else)
Динамическое выделение памяти
Препроцессорные директивы

🎓 Учебные цели
Проект разработан в образовательных целях для изучения:
Принципов работы компиляторов
Лексического и синтаксического анализа
Трансляции между языками программирования
Работы с Flex и Bison

